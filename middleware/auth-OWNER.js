export default async ({ $auth, redirect, state }) => {
    console.log(state);
    // if ($auth.user.role == "OWNER") {
    //     redirect("/dashboard");
    // } else {
    //     redirect("/");
    // }
};
